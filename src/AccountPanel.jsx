import React,{useEffect,useState} from 'react';
export default function AccountPanel({cloud,state,onClose}){const[name,setName]=useState('My interior');useEffect(()=>{if(state.active)setName(state.active.name);},[state.active?.id]);useEffect(()=>{if(state.user)cloud.list();},[state.user?.id]);return <div className="land-overlay"><section className="land-panel account-panel" role="dialog" aria-modal="true" aria-labelledby="designs-title"><button className="land-close" aria-label="Close my designs" onClick={onClose}>×</button><h2 id="designs-title">My designs</h2>{!state.configured?<><p>Google sign-in and private cloud saving aren’t connected yet.</p><p>The house owner needs to finish the Google and Supabase setup. You can keep designing here and use Save colors / Save moulding for local backups.</p><button disabled>Continue with Google</button></>:state.loading?<p role="status">Checking sign-in…</p>:!state.user?<><p>Sign in to save private interiors and open them on another device.</p><button className="google-signin" disabled={state.busy} onClick={()=>cloud.signIn()}>Continue with Google</button><p>Signing in opens your own browser draft. Your guest draft stays available after signing out.</p></>:<><p>Signed in as <strong className="account-email">{state.user.email}</strong></p><p>Only your account can access these saved designs. Duplicate the original house to start an independent copy. Saved copies include wall colors, moulding, land, furniture visibility and edit history.</p><p><strong>{state.active?state.active.name:"Unsaved copy"}</strong>{state.dirty?" · Unsaved changes":""}</p><button disabled={state.busy} onClick={()=>cloud.duplicate(true)}>Duplicate baseline house</button><button disabled={state.busy} onClick={()=>cloud.duplicate(false)}>Duplicate current design</button><form onSubmit={e=>{e.preventDefault();cloud.save(name);}}><label>Design name<input aria-label="Design name" value={name} maxLength={80} onChange={e=>setName(e.target.value)} required/></label><button className="primary" disabled={state.busy||!name.trim()} type="submit">{state.active?"Save changes":"Save private copy"}</button></form><div className="account-list-heading"><h3>Saved designs</h3><button disabled={state.busy} onClick={()=>cloud.list()}>Refresh</button></div>{state.busy&&<p role="status">Working…</p>}{!state.items.length&&!state.busy&&<p>No saved designs loaded yet. Create your first private copy or refresh.</p>}<ul className="design-list">{state.items.map(d=><li key={d.id}><div><strong>{d.name}</strong><small>{new Date(d.created_at).toLocaleString()}</small></div><button disabled={state.busy} onClick={()=>cloud.open(d.id)}>Open</button></li>)}</ul>{state.more&&<button disabled={state.busy} onClick={()=>cloud.list(true)}>Load more</button>}{cloud.previous&&<button disabled={state.busy} onClick={()=>cloud.restore()}>Restore previous draft</button>}<p>Use Save changes to keep this copy and its history in your account. Opening another copy replaces the working draft; Restore previous draft brings it back. The original house always remains available.</p><button disabled={state.busy} onClick={()=>cloud.signOut()}>Sign out</button></>}<h3>Edit history</h3><p>Up to 50 recent changes. Save your private copy before leaving to keep undo and redo steps. Unsaved history lasts only for this session.</p><button disabled={state.busy||!state.history?.cursor} onClick={()=>cloud.history.go(state.history.cursor-1)}>Undo</button><button disabled={state.busy||state.history?.cursor>=state.history?.entries.length-1} onClick={()=>cloud.history.go(state.history.cursor+1)}>Redo</button><ol className="design-history">{state.history?.entries.map((entry,index)=><li key={index}><button disabled={state.busy||index===state.history.cursor} aria-current={index===state.history.cursor?'step':undefined} onClick={()=>cloud.history.go(index)}>{index===state.history.cursor?'● ':''}{entry.label}</button></li>)}</ol>{state.error&&<p className="account-error" role="alert">{state.error}</p>}{state.message&&<p role="status">{state.message}</p>}</section></div>;}
